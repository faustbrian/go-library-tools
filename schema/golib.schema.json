{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/faustbrian/go-library-tools/main/schema/golib.schema.json",
  "title": "golib repository configuration",
  "description": "Repository-specific policy consumed by the golib CLI.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "tool_version"
  ],
  "properties": {
    "schema_version": {
      "const": 1,
      "description": "Configuration schema version."
    },
    "tool_version": {
      "type": "string",
      "pattern": "^v[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "Exact released golib CLI version."
    },
    "tool_checksums_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 digest of the release checksums.txt asset."
    },
    "manifest": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "modules": {
          "$ref": "#/$defs/repositoryPath",
          "default": "modules.json"
        },
        "packages": {
          "$ref": "#/$defs/repositoryPath",
          "default": "packages.json"
        }
      }
    },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "root": {
          "$ref": "#/$defs/repositoryPath",
          "default": ".verification"
        }
      }
    },
    "mutation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "root": {
          "$ref": "#/$defs/repositoryPath",
          "default": ".verification/mutation"
        }
      }
    },
    "api": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "baselines": {
          "type": "array",
          "items": { "$ref": "#/$defs/apiBaseline" }
        }
      }
    },
    "runtimes": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "deno": {
          "type": "string",
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
          "description": "Exact Deno version required by repository tests."
        },
        "zsh": {
          "const": "5.9",
          "description": "Exact supported zsh runtime required by repository tests."
        }
      }
    },
    "operations": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["module", "gate", "steps"],
        "properties": {
          "module": { "$ref": "#/$defs/repositoryPath" },
          "gate": {
            "enum": ["api", "benchmark", "conformance", "docs", "fuzz", "interoperability", "test"]
          },
          "steps": {
            "type": "array",
            "minItems": 1,
            "items": { "$ref": "#/$defs/step" }
          }
        },
        "allOf": [
          {
            "if": {
              "properties": { "gate": { "const": "test" } },
              "required": ["gate"]
            },
            "then": {
              "properties": {
                "steps": {
                  "items": {
                    "not": {
                      "anyOf": [
                        { "required": ["benchmark"] },
                        { "required": ["fuzz"] },
                        { "required": ["budget"] }
                      ]
                    }
                  }
                }
              }
            }
          }
        ]
      }
    }
  },
  "$defs": {
    "apiBaseline": {
      "type": "object",
      "additionalProperties": false,
      "required": ["module", "mode", "path"],
      "properties": {
        "module": { "$ref": "#/$defs/repositoryPath" },
        "mode": { "enum": ["apidiff", "go-doc"] },
        "path": {
          "allOf": [
            { "$ref": "#/$defs/repositoryPath" },
            { "not": { "const": "." } }
          ]
        },
        "packages": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^(?:\\.|\\./[^/]+(?:/[^/]+)*)$",
            "not": {
              "anyOf": [
                { "pattern": "\\\\" },
                { "pattern": "(?:^|/)\\.{1,2}(?:/|$)" },
                { "pattern": "(?:^|/)\\.\\.\\.(?:/|$)" }
              ]
            }
          }
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "mode": { "const": "go-doc" } },
            "required": ["mode"]
          },
          "then": {
            "required": ["packages"],
            "properties": { "packages": { "minItems": 1 } }
          }
        },
        {
          "if": {
            "properties": { "mode": { "const": "apidiff" } },
            "required": ["mode"]
          },
          "then": { "not": { "required": ["packages"] } }
        }
      ]
    },
    "step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type"],
      "properties": {
        "type": { "enum": ["go-test", "make"] },
        "makefile": {
          "allOf": [
            { "$ref": "#/$defs/repositoryPath" },
            { "not": { "const": "." } }
          ]
        },
        "target": {
          "type": "string",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9_.:/-]*$"
        },
        "packages": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^(?:\\.|\\./[^/]+(?:/[^/]+)*)$",
            "not": {
              "anyOf": [
                { "pattern": "\\\\" },
                { "pattern": "(?:^|/)\\.{1,2}(?:/|$)" },
                { "pattern": "(?:^|/)\\.\\.\\./.+" }
              ]
            }
          },
          "default": ["./..."]
        },
        "run": { "type": "string" },
        "benchmark": { "type": "string" },
        "fuzz": { "type": "string" },
        "budget": {
          "type": "string",
          "description": "Positive Go duration or iteration count ending in x; defaults to 100ms for benchmarks and 10000x for fuzzing."
        },
        "count": { "type": "integer", "minimum": 0, "default": 1 },
        "timeout": { "type": "string", "default": "20m" }
      },
      "allOf": [
        {
          "if": {
            "properties": { "type": { "const": "make" } },
            "required": ["type"]
          },
          "then": {
            "required": ["makefile", "target"],
            "not": {
              "anyOf": [
                { "required": ["packages"] },
                { "required": ["run"] },
                { "required": ["benchmark"] },
                { "required": ["fuzz"] },
                { "required": ["budget"] },
                { "required": ["count"] }
              ]
            }
          },
          "else": {
            "not": {
              "anyOf": [
                { "required": ["makefile"] },
                { "required": ["target"] }
              ]
            }
          }
        }
      ]
    },
    "repositoryPath": {
      "type": "string",
      "minLength": 1,
      "not": {
        "anyOf": [
          {
            "pattern": "^(?:/|[A-Za-z]:[\\\\/])"
          },
          {
            "pattern": "(^|[\\\\/])\\.\\.([\\\\/]|$)"
          }
        ]
      }
    }
  }
}
